import 'package:oxygen/oxygen.dart';

class RestController {
  final Entity restingEntity;
  RestController(this.restingEntity);

  /// reset flux
  /// regenerate health
  /// regenerate boon
  /// reset actions that are n/rest
  /// reset dyingstate threshold
  void performRest() {
    UnimplementedError();
  }

  /// set spentFlux to 0
  /// reset actions that are 1/pause
  void performPause() {
    UnimplementedError();
  }
}